// Usuarios - La comanda Juan Marcos Vallejo.
// -- 22/04/19 --

(function (self, $, undefined) {

    self.Inicializar = function () {
        // Inicialización.

    };

    // Métodos públicos.

    self.Nueva = function () {
        traerComponentes();
    }

    self.Nueva = function () {
        traerComponentes();
    }

}(window.Comanda.Usuarios = window.Comanda.Usuarios || {}, jQuery));

jQuery(function () {
    window.Comanda.Usuarios.Inicializar();
});